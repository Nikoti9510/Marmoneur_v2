---
import Post from "../../layouts/Post.astro";
import { getCollection, render } from 'astro:content';

const allPost = await getCollection("content", ({ data }) => {
  return data.draft !== true;
});

const { Content } = await render(allPost);
---

<Post {...allPost.data}>
	<Content />
</Post>