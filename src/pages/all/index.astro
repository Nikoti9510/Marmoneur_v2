---
import { getCollection } from 'astro:content';
import { Debug } from 'astro:components';

const posts = await getCollection('content', ({ data }) => {
  return data.draft === false;
});
---

{posts.map((post) => (
      <li><a href={`/content/${post.data.id}`}>{post.data.title}</a></li>
))}