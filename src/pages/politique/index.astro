---
import { getCollection } from 'astro:content';
import { Debug } from 'astro:components';

const posts = await getCollection('content', ({ data }) => {
  return data.draft === false && data.tags == "politique";
});
---

<Debug {posts.url}/>