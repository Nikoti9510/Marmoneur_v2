---
import Post from "../../layouts/Post.astro";
import { getCollection, Debug } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection("autres");
  return posts.map((post) => ({
    params: { id: post.id }, // Pourquoi il y a .md dans le nom de mes dossiers lors du build ?
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();
---

<Post {...post.data}>
  <Debug {post.id} />
  <Content />
</Post>