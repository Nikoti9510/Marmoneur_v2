---
import { getCollection } from 'astro:content';
import Layout from "../../layouts/Layout.astro";

const posts = await getCollection('autres', ({ data }) => {
  return data.draft === false;
});
---
<Layout>
  {posts.map((post) => (
        <li><a href={`/jdr/${post.id}`}>{post.data.title}</a></li>
  ))}
</layout>